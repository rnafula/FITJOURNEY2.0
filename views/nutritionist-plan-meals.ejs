<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Meal Plans</title>
  <link rel="stylesheet" href="nutri.css">
  <style>
    :root{
      --accent: #6c93db;
      --muted: #f1f5f9;
      --card-border: #e6eefc;
      --text: #233044;
    }
    body{
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      margin: 0;
      background: #f7fafc;
      color: var(--text);
    }
    header { margin-bottom: 8px; }
    .text { text-align: center; margin: 8px 0; }

    .plans-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 16px;
      width: 90%;
      max-width: 1100px;
      margin: 20px auto;
    }

    .plan-card {
      background: white;
      border: 1px solid var(--card-border);
      border-radius: 10px;
      padding: 16px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.05);
    }

    .plan-title {
      color: var(--accent);
      font-weight: 700;
      font-size: 1.1rem;
      margin: 0 0 8px;
    }
    .plan-desc { margin: 0 0 8px; font-size: 0.95rem; color: #475569; }
    .plan-meta { font-size: 0.9rem; color: #334155; margin-bottom: 8px; }

    .meal-day {
      background: var(--muted);
      border-radius: 6px;
      padding: 8px;
      margin: 6px 0;
    }
    .meal-day strong { color: var(--accent); }
    .meal-item { margin: 4px 0; font-size: 0.9rem; }
  </style>
</head>
<body>
  <%- include("_header.ejs") %>

  <h1 class="text">My Meal Plans</h1>
  <p class="text"><%= user ? user.username : "No user in session" %></p>

  <div class="plans-grid">
    <% if (mealPlans && mealPlans.length > 0) { %>
      <% mealPlans.forEach(plan => { %>
        <div class="plan-card">
          <p class="plan-title"><%= plan.title %></p>
          <p class="plan-desc"><%= plan.description %></p>
          <p class="plan-meta"><strong>Duration:</strong> <%= plan.duration_days %> days</p>

          <% if (mealsByPlan[plan.id] && mealsByPlan[plan.id].length > 0) { %>
            <% 
              let groupedByDay = {};
              mealsByPlan[plan.id].forEach(meal => {
                if (!groupedByDay[meal.day_number]) groupedByDay[meal.day_number] = [];
                groupedByDay[meal.day_number].push(meal);
              });
            %>
            <% Object.keys(groupedByDay).forEach(day => { %>
              <div class="meal-day">
                <strong>Day <%= day %></strong>
                <% groupedByDay[day].forEach(meal => { %>
                  <div class="meal-item">
                    🍽 <%= meal.meal_type %>: <%= meal.description %>
                  </div>
                <% }) %>
              </div>
            <% }) %>
          <% } else { %>
            <p>No meals added yet.</p>
          <% } %>
        </div>
      <% }) %>
    <% } else { %>
      <p style="text-align:center; width:100%;">No meal plans created yet.</p>
    <% } %>
  </div>
</body>
</html>
