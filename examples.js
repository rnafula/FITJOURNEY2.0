let numbers = [2, 3, 4, 5]
const doubled = numbers.map(number => number * 2)
console.log(doubled);

const users = [
  { id: 1, name: "Alice"},
  { id: 2, name: "Bob"},
  { id: 3, name: "Charlie"}
];
 const ids =users.map(user=>user.id)
 console.log(ids);
 